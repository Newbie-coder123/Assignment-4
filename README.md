File 1: It contains the code to check if a file exsits or not. If it does then the contents of the file are printed line by line

File 2: It contanins the code to read, write or append the file as much as the user wants. If the file does not exsists than the program creates a new file. If it exsist than the operation are performed on the exsisting file
